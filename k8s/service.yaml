apiVersion: v1
kind: Service
metadata:
  name: walking-in-bevy-server
  labels:
    app.kubernetes.io/name: walking-in-bevy-server
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: walking-in-bevy-server
  ports:
    - name: game-udp
      port: 5000
      targetPort: game-udp
      protocol: UDP
